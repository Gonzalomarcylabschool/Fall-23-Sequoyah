// Actual garbage
const fStuNms = stus.map(s => s.n)

// Chef's kiss
const filteredStudentNames = students.map(student => {
  return student.name;
});

// BE AWARE OF CONVENTIONS
/*
  arr === array
  num === number
  i === index
  idx === index
  len = length
  e === error OR event (Use context clues)
  el = htmlElement
  init = initiate
*/