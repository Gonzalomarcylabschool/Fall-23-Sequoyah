// Just like strings!
const arr = ['a', 'b', 'c', 'd'];

console.log(arr.length); // 4
console.log(arr[0]); // a
console.log(arr[arr.length - 1]); // d

// logs each letter
for (let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}